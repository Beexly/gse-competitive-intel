# HP-14 — PRIORITY 0 TONIGHT: v5.2.8 IMPLEMENTED, edge-based NFL/NCAAF moneyline + totals publish

Founder YES recorded 2026-09-09 ("do not compress"; NFL Week 1 kickoff is less than 24 hours out).
A cloud session could not be spawned for this, so Hermes owns it. Same laws as HP-1..HP-9
(`LAUNCH/13-HERMES-DISPATCH-2026-09-08.md`). Branch `hermes/v528-week1` off `origin/main`.
Order for tonight is now: **HP-14**, HP-3, HP-13, HP-10, HP-4, HP-12, HP-11, then the rest.

1. Read `docs/calibration-proposals/2026-09-05-market-anchored-display-probability-v5.2.8.md` and
   `docs/ops/NFL_WEEK1_COVERAGE_2026-09-08.md`. Root cause of the empty NFL moneyline/totals board:
   `packages/prediction-engine/src/scoring.ts` drops moneylines at the 0.58 fair-prob floor (~:965) and
   the 50 confidence floor (~:1033); totals die at the priced-set / 0.55 vote / 50 confidence gates
   (~:703, :737, :811).
2. Implement the proposal: displayed probability = market-anchored p (basis `market_anchored_v2`, the
   basis the calibration path in `apps/web/lib/calibration/*` already uses). Publish NFL and NCAAF
   moneylines and totals on POSITIVE market-relative edge e = p - q (q = de-vigged book probability),
   never on the 0.58 fair-prob floor, never on negative edge (ledger C-197 and C-255 doctrine). Spreads
   keep their current path. Real book lines only; `MIN_BOOKMAKERS` stays.
3. Same commit: proposal front-matter `status: IMPLEMENTED`, `modelVersion: v5.2.8`, and
   `packages/prediction-engine/src/constants.ts` `MODEL_VERSION = "v5.2.8"` so
   `node scripts/guardrails/model-freeze.mjs` passes. Do not touch scripts/guardrails, .github,
   .claude, prisma, any .env.
4. Red-checked vitest tests in packages/prediction-engine: positive-edge ML publishes; negative-edge
   does not; sub-0.58 fair-prob with positive edge publishes; the same three for totals.
5. Verify block: typecheck 0, lint 0, `npx vitest run packages/prediction-engine`, model-freeze OK
   v5.2.8, `npm run guardrails` green, `npm run build` exit 0.
6. Ledger: claim row C-275 "v5.2.8 IMPLEMENTED - edge-based NFL/NCAAF ML+totals publish" Owner hermes
   in the first commit; DONE with SHA at the end; `node scripts/ops/check-agent-ledger.mjs` green.
7. Push `hermes/v528-week1`, open a READY PR titled "engine: v5.2.8 market-anchored probability,
   edge-based NFL/NCAAF moneyline and totals publish"; body lists each gate changed with line refs
   and the test names. Two attempts; then BLOCKED with the exact error and a draft PR.
