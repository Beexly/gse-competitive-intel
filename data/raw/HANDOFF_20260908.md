# Handoff — 2026-09-08, launch-decision session

For the session taking over. The founder is bringing Firecrawl research into that
session. Everything below traces to a command that was run or a `file:line` that was read;
where something was not verified it says so. Nothing here authorises flipping a gate,
weakening a guard, or publishing an unearned claim.

---

## 0. Start here

Read in this order, then stop reading and work:

1. This file.
2. `docs/ops/EDGE_DOCTRINE_AUDIT_2026-09-08.md` — why the board was ranking on a coin flip.
3. `docs/ops/audit-2026-09-08/production-truth-2026-09-08.md` — the live numbers, and the
   one that matters most: our own surface names the market as the best score we have.
4. `docs/ops/audit-2026-09-08/README.md` — then whichever of the ten dimension reports you
   need. **They are pre-verification drafts. A finding there is a candidate, not a fact.**

Branch: `claude/sports-launch-round2-fixes-hk7kv9`. PR **#720**, open, draft.
Head at handoff: `c641f37e7`. Verified green on it: typecheck 0, lint 0, guardrails 26/26,
full suite **17,554 passed / 0 failed**, ledger guard OK, `npm run build` exit 0.

---

## 1. The launch call

**Postpone.** Not on polish. On the premise. Three findings, each independently
sufficient, and they compound.

### 1a. The board did not rank on edge. This is now FIXED in code.

The strategy doctrine (`_HANDOFF-to-coding-agent.md`, intel repo) names one rule above all
others: fire and rank on `e = p - q`, never on confidence, because confidence-gating is
"scores24's fatal error." Measured at the start of this session: the live plan set
`selectiveRecommended.edge = null`, so public selection was `|p - 0.5| >= 0.12`, and **387
of 428 published moneyline picks carried no market probability at all**, so `rawEdge` on
those rows was literally `trueProb - 0.5`.

It was never a data problem. **232 of those 387 had a real bookmaker quoting both sides in
our own `odds` table at or before the pick's own `generatedAt`.** The signal slate never
looked. C-253 wired the same resolver the calibration loader uses into the publish path;
measured ceiling on the moneyline board goes from 41/428 to 273/428.

**Still open and founder-gated:** the tuner and the runtime resolve `marketP` from
different places. The sweep uses `independentEdge.marketFairProb ?? factorBreakdown.marketFairProb ?? receipt.marketFairProb`;
`/api/picks/route.ts` reads only the top-level `factorBreakdown.marketFairProb`, present on
48 of 435. A threshold tuned on one population would be enforced on another. It is a no-op
while `edge` is null, which is why it was left.

### 1b. The settled record is contradicted by the feed it came from. Founder-owned.

C-247: 25/169 MLB, 14/48 MLS, 2/37 NCAAF stored FINAL scores disagree with ESPN's own API;
68 settled published picks sit on them; 8 moneyline results are the opposite of what
happened. Every calibration number in the repo is a measurement over those inputs.

**There is now a command for it** (C-254/C-256): `npm run ops:repair-scores`. Dry run by
default. `--execute` applies it one transaction per game. Writes exactly four fields, and a
test enforces that by scanning every Prisma `data:` block. Re-grades through the engine's
own `calculatePickResult` and `selectGradingLine`. Refuses a game whose settled picks
cannot all be re-graded, and refuses to run at all while a write-once
`PickSignalSnapshot.settlementResult` or `PickSettlementEvent.result` would be left
contradicting a corrected result, unless the operator passes
`--accept-stale-derivatives`. **Not run against production in this session.**

### 1c. Our own bake-off names the market as the best score we produce.

`/api/ops/public-surface-truth` carries `"bestScore": "marketFairProb"`. The market's
de-vigged probability beats every score we generate on Brier (0.2337 vs our best 0.2474),
ECE (0.0280 vs 0.0838) and **Murphy resolution** (0.0185 vs 0.0112). Resolution is ranking
power. On moneylines the confidence score's separation is **-0.0018** at n 745.

Two confounds recorded rather than buried: the scores are not on an identical row set
(market coverage 0.774 vs confidence 1.000), so the *size* of the gap is not established;
and the outcomes are the C-247 record. Neither rescues the finding.

**The measurement nobody has run, and the next session should:** put all four scores on an
identical row set and re-read the gap.

---

## 2. Founder questions, answered from evidence

- **Firecrawl on PropFinder?** A previous session did, deeply. `_propfinder-teardown-final.md`,
  906 lines, PF Rating reconstructed from their public JS. The Firecrawl MCP bridge was
  **disconnected in this session** despite being enabled on the account, so wave 2 used
  WebSearch/WebFetch instead.
- **Aligned to those findings?** No, and the teardown's own "do not copy" list opens with
  **client-side scoring** — which is exactly what our DFS optimizer does.
- **Hundreds of simulated tests for the optimizer?** Zero. No Monte-Carlo anywhere in the
  DFS path. But see 3a: the gap is smaller than it looks.
- **Lineup cap, tier-gated?** No cap, no gate. A client slider, `min={1} max={20}`, computed
  in the browser, no server route, no entitlement check. `wave2/_TIER-DESIGN.md` proposes
  the concrete gate.

---

## 3. What the research changed

### 3a. The optimizer gap is an input and two layers, not a rewrite

SaberSim's CEO, on their own site: *"for every lineup we build you, we randomly choose a
bucket of sims and optimize based on the average score for each player in that bucket."*
That is a **deterministic point-projection solve, the same shape as ours**, run many times
against vectors drawn from a 100,000-run simulation. Our exact DP kernel is right. What we
lack: a sims-by-players score matrix, a contest-field simulator, a portfolio selector.

### 3b. Their pricing is the category's product boundary

Starter $97 (**500 lineups per build**), Pro $197 (5,000), Ultimate $297 (contest-specific
sims and the ROI suite). Their own help centre: *"you'll need Ultimate to access detailed,
contest-specific simulations."* **Our Pro tier is $14.99. The category floor is $97.**

### 3c. The seam is wide and structural

For a product whose pitch is "we compute your ROI 100,000 times," SaberSim publishes zero
projection accuracy, zero calibration, zero aggregate ROI. What they publish is 2,084
winner screenshots totalling $38.56M, concentrated in 746 unique names with one holding 31
cards, and **no "results not typical" disclaimer anywhere in the HTML**. Their own FAQ:
*"Why do my lineups look different every time I build? Each build samples a new set of
simulations."* **Their product cannot reproduce its own output.** Ours is documented as
"no randomness, no local search, no restarts." Seed the sim draw, store the seed, publish
it with the lineup, and a GSE lineup is re-derivable by a third party. Nobody in the
category ships that.

---

## 4. Shipped this session, all on PR #720

| Row | What was wrong |
|---|---|
| C-242 | DFS exposure bound enforced but never disclosed |
| C-243 | Projections loaded twice at startup (no single-flight) |
| C-244 | Satellite stats refresh had no daily window; prior-season fallback ungated |
| C-245 | A failed reload left a stale provider registered |
| C-246 | Dashboard card counted one thing and was labelled another |
| C-247 | Stored finals contradicted by the ESPN feed — **escalated, founder-owned** |
| C-248 | Built the read-only detector for it |
| C-250 | The tool built to catch mislabelled numbers shipped a mislabelled number |
| C-251 | `/fantasy/studio` was prerendered, freezing its live-data claim at build time |
| C-252 | Pick card claimed a book price on picks that have none |
| **C-253** | **The market price was in our own database and the slate published a coin flip** |
| C-254 | C-247 had a detector and no lever; built `ops:repair-scores` |
| C-255 | A pick the market prices above us is not a LEAN (5 Devin findings on my own work) |
| C-256 | The repair tool could manufacture the contradiction it exists to remove |

---

## 5. THE RECURRING DEFECT CLASS — read this before writing any code here

**A count, label, badge or flag whose stated meaning is not what it measures.**

C-241, C-246, C-250, C-251, C-252, and twice inside C-255. C-250 was committed *inside the
instrument built to detect the class*. C-255 fixed a counter that over-counted, in the
counter added to measure the fix for the class.

**Assume more exist.** Red-check every guard you write: mutate the fix, confirm the test
fails, restore. Every fix in this session was red-checked and the counts are in the commit
messages. When a guard fires too broadly, **narrow its context, never its power** (law 9);
C-254 and C-255 each have a worked example of doing exactly that.

---

## 6. Founder-owned, untouched, blocking

1. **C-247 repair.** Needs database writes and a root cause. More than one mechanism is at
   work: the MLB consecutive-day series-propagation pattern does not explain MLS.
2. **PROVEN eligibility structural conflict.** Settlement grace 6h
   (`settlement-health.ts:58`); `HEALTHY` requires `overduePending === 0` exactly (line 86);
   the zero-sit lane deliberately waits 24h past kickoff (`zero-sit-lane.ts:104`). One
   ungradeable pick forces eligibility RED for up to 18 continuous hours **by design**. No
   agent may touch any of those three constants: each would turn the light green without
   making anything more true.
3. **The edge threshold**, and the tuner/runtime `marketP` divergence in 1a.
4. **v5.2.7 stratum caveat.** The pooled ECE sits below every model version it is built
   from; the deployed version measures roughly twice the floor on its own rows.
5. **C-224** (four brierScore surfaces).
6. **Pricing.** `_TIER-DESIGN.md` is a proposal. No dollar amount in
   `pricing-phases.ts` may be changed by an agent.

---

## 7. Open, unblocked, and worth doing next

Ordered by leverage.

1. **The identical-row-set bake-off** (§1c). One measurement, decides whether the model
   beats the market anywhere.
2. **Work the ten audit reports** in `docs/ops/audit-2026-09-08/`. They are drafts; verify
   at the cited `file:line` before acting. All ten dimensions landed.
3. **NFL Week 1 has no board.** Read live at 15:38 UTC: 6 NFL games in the next 72 hours,
   **2 published picks, both spreads, zero moneylines, zero totals**. MLB carries the
   product with 56. Root-cause it before the weekend.
4. **`wave2/_BUILD-PLAN.md` and `_SCORE-ALIGNMENT.md` did not land.** `_TIER-DESIGN.md` and
   all eleven dossiers did. Re-run those two synthesis passes over
   `/home/user/gse-competitive-intel/wave2/*.md`.
5. **The api-no-store ratchet's 45 remaining routes** (`KNOWN_REMAINING` in
   `apps/web/__tests__/api-no-store-ratchet.test.ts`). PRO-gated, fail closed, may only
   shrink.
6. **CodeRabbit docstring coverage** warns at 52.38% against an 80% threshold. It is a
   warning, not a CI failure. Bulk-generating docstrings to satisfy it is not the answer.

---

## 8. Do NOT redo

- C-247's measurement. The tool exists: `npm run ops:verify-scores -- --sport=<x> --days=<n>`.
- The edge finding. `EDGE_DOCTRINE_AUDIT_2026-09-08.md` has the SQL, the counts, the lines.
- PropFinder, LineStar, FantasyPros, scores24, FantasyGuru, SaberSim, Stokastic,
  FantasyLabs, Awesemo/RotoGrinders, OddsJam/Unabated, PrizePicks/Underdog, Sleeper/DK/FD,
  the open-source stack, the simulation literature, the forecast-scoring literature.
- The quintile analysis of `rawEdge` against outcome. It was run, it was invalid (market
  means averaged over a sparse minority of rows), and it is withdrawn in §2 of the edge
  audit.
- Any of the above by changing a floor, a threshold, a grace window, or a health condition.

---

## 9. For the Firecrawl work the founder is bringing

The brief is committed at `/home/user/gse-competitive-intel/prompts/FIRECRAWL-TOTAL-DOMINANCE.md`
(intel repo, same branch). Sixty-plus targets across six tiers, method ranked by yield.

**Where new dossiers go:** `/home/user/gse-competitive-intel/wave3/<slug>.md`, one per
target, every factual line carrying the URL it came from.

**The standard is set by `_propfinder-teardown-final.md`** — it reconstructed a competitor's
entire proprietary scoring formula from the JavaScript they serve every anonymous visitor,
and checked their "18,000+ community members" claim against Discord's public API (16,735).

**Public surfaces only.** No authentication, no paywall crossing, no `Disallow`ed paths, no
endpoint scanning. That is not squeamishness: intel obtained past a login is intel legal
review makes us delete, and it contaminates the source-rights registry that lets the rest
ship. Everything the brief asks for is legal, public, and yields more.

---

## 10. Session mechanics

- **Two research workflows may still be running** when this session ends. If
  `docs/ops/audit-2026-09-08/` or `wave2/` looks partial, the run was interrupted; this
  container already restarted once today and killed four background tasks.
- **PR #720 is watched.** Devin and CodeRabbit review each push. Devin found six real bugs
  in my own work today, and every one was correct. Treat its findings as bug reports:
  verify at the cited line, then fix. Both intel-repo and Sports work is on the same
  branch name.
- Commit incrementally and push. A partial directory on the remote beats a complete one
  that never leaves the box.
